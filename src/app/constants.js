const ActionStatus = {
    START: 'START',
    COMPLETE: 'COMPLETE',
    ERROR: 'ERROR'
}

const GeneralConstants = {
    FETCH_INTERVAL_MILLIS: 10000
}

module.exports = {ActionStatus, GeneralConstants}