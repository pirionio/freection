const React = require('react')
const {Component} = React

class App extends Component {
    render () {
        return (
            <div>Hello World!</div>
        )
    }
}

App.propTypes = {
}

module.exports = App